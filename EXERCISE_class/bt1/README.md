# Student Management ES6

```powershell
npm start
```

- `models`: các class `Person` và `Student`.
- `services`: thêm điểm và đánh giá bằng Promise.
- `utils`: Rest Parameter, `filter`, `map` và `reduce`.
- `app.js`: tạo dữ liệu và gọi các chức năng.
