# Exercise 4 - JSX and ES6

```powershell
npm install
npm start
```

Ứng dụng gồm phần JSX theo ảnh mẫu và toàn bộ bài tập ES6: `find`, `filter`, `every`, `some`, `forEach`, `map`, `sort`, `reduce`, destructuring, rest parameter, class inheritance và Promise.
